.chat-group-user
  -# %input.hidden_field { :group_user, name: 'group[user_ids][]', value: user.id }
  %input{name: "group[user_ids][]", type: "hidden", value: user.id}
  %p.chat-group-user__name 
    = user.name
  - unless user.name == current_user.name
    %p.user-search-remove.chat-group-user__btn.chat-group-user__btn--remove.js-remove-btn
      削除
